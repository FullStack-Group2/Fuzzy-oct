import ChatPage from "./ChatPage";


const Home = () => {
  return <>
    <button onClick={() => {
      // Open chat page
      // navigate, have vendorid
    }}>Chat to vendor</button>
  </>;
};

export default Home;
